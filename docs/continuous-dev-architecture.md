# 持续开发架构设计文档

## 🎯 核心问题

> 如何在已有代码仓库上，使用多 Agent 协同持续开发新需求，同时**不破坏现有功能、不引入新 bug**？

## 📚 设计来源

本架构融合了两个核心来源：
1. **项目现有架构** - 蜂王-蜜蜂模型、任务树、TDD 循环
2. **Cursor 团队经验** - [Scaling Agents](https://cursor.com/cn/blog/scaling-agents)

### Cursor 关键经验

| 经验               | 应用                                          |
| ------------------ | --------------------------------------------- |
| 规划者和执行者分离 | `AgentCoordinator` (Queen) + `WorkerExecutor` |
| 周期性重启对抗漂移 | `CycleResetManager` - 每 N 个任务或时间后重置 |
| Worker 自治        | Worker 独立执行，不需要中央协调解决冲突       |
| 简化优先           | 去掉不必要的中间角色，减少瓶颈                |
| 模型角色匹配       | Planner 用强模型，Worker 用快模型             |
| 评审 Agent         | `ReviewResult` - 周期评审决定是否继续         |

---

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        持续开发编排器 (ContinuousDevOrchestrator)            │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          ▼                           ▼                           ▼
┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│   代码库分析器       │  │   影响分析器         │  │   蓝图管理器         │
│ (CodebaseAnalyzer)  │  │ (ImpactAnalyzer)    │  │ (BlueprintManager)  │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘
          │                           │                           │
          │   ┌───────────────────────┴───────────────────────┐   │
          ▼   ▼                                               ▼   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Agent 协调器 (AgentCoordinator)                     │
│                                                                             │
│   ┌───────────────┐                                   ┌───────────────┐     │
│   │   蜂王 Queen   │─────────协调────────────────────▶│   Workers     │     │
│   │   (Planner)   │                                   │  (Executors)  │     │
│   └───────────────┘                                   └───────────────┘     │
│                                                               │             │
│                                                               ▼             │
│                                              ┌─────────────────────────┐    │
│                                              │   回归测试门禁           │    │
│                                              │   (RegressionGate)      │    │
│                                              └─────────────────────────┘    │
│                                                               │             │
│                                                               ▼             │
│                                              ┌─────────────────────────┐    │
│                                              │   周期重置管理器         │    │
│                                              │   (CycleResetManager)   │    │
│                                              └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📋 完整开发流程

### 阶段 1️⃣ 代码库理解

```typescript
const analyzer = new CodebaseAnalyzer({ rootDir: projectPath });
const { codebase, blueprint } = await analyzer.analyzeAndGenerate();
```

**产出**：
- `CodebaseInfo` - 模块结构、依赖关系、技术栈
- 逆向蓝图 (Reverse Blueprint) - 描述现有系统的约束

---

### 阶段 2️⃣ 影响分析

```typescript
const impactAnalyzer = new ImpactAnalyzer({ projectRoot });
await impactAnalyzer.initialize(codebaseInfo);

const impact = await impactAnalyzer.analyzeRequirement(
  "增加用户邀请功能",
  reverseBlueprint
);
```

**产出**：
- `ImpactAnalysisReport` - 影响范围、风险评估
- `SafetyBoundary` - 安全边界（允许/禁止操作的路径）
- `RegressionScope` - 回归测试范围

---

### 阶段 3️⃣ 增量蓝图生成

```typescript
const incrementalBlueprint = await blueprintManager.createIncrementalBlueprint(
  reverseBlueprint,    // 基础约束
  requirement,         // 新需求
  impactAnalysis       // 影响分析（可选）
);

const taskTree = taskTreeManager.generateFromBlueprint(incrementalBlueprint);
```

**产出**：
- 增量蓝图 = 现有约束 + 新功能模块
- 任务树 - 可执行的任务层级结构

---

### 阶段 4️⃣ 安全执行

```
┌────────────────────────────────────────────────────────────────────┐
│                         三道护栏机制                                │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  护栏 1: 边界检查器 (BoundaryChecker)                              │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  Worker 只能在 SafetyBoundary.allowedPaths 内操作           │   │
│  │  禁止访问 SafetyBoundary.forbiddenPaths                     │   │
│  │  核心文件修改需要人工审核                                    │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  护栏 2: TDD 循环 (TDDExecutor)                                    │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  测试先行：Queen 生成验收测试，Worker 不能修改               │   │
│  │  Worker 必须让测试通过才能提交                               │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  护栏 3: 回归测试门禁 (RegressionGate)                             │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  每次 Worker 提交前运行：                                    │   │
│  │  1. 新功能测试（Worker 写的）                                │   │
│  │  2. 回归测试（现有全部测试）                                 │   │
│  │  任何测试失败 → 拒绝提交，Worker 必须修复                    │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

### 阶段 5️⃣ 周期评审与重置

```typescript
// Cursor 经验：周期性重置对抗漂移
const cycleManager = new CycleResetManager(taskTreeManager);

// 触发条件
cycleManager.config.triggers = {
  taskCompletionCount: 10,      // 每完成 10 个任务
  maxRunTimeMinutes: 60,        // 或运行 1 小时
  contextTokenThreshold: 100000,// 或上下文超 10 万 token
  consecutiveFailures: 3,       // 或连续失败 3 次
};

// 周期评审
const review = await cycleManager.performReview(taskTree);
if (review.shouldReset) {
  await cycleManager.performReset(taskTree, blueprint, reason);
  // 从干净状态重新开始
}
```

---

## 🔧 新增组件

| 组件               | 文件                             | 作用                                     |
| ------------------ | -------------------------------- | ---------------------------------------- |
| **影响分析器**     | `impact-analyzer.ts`             | 分析新需求对现有代码的影响，设置安全边界 |
| **回归测试门禁**   | `regression-gate.ts`             | 守门人，确保不破坏现有功能               |
| **周期重置管理器** | `cycle-reset-manager.ts`         | 对抗漂移，周期性评审和重置               |
| **持续开发编排器** | `continuous-dev-orchestrator.ts` | 串联所有组件的主入口                     |

---

## 💻 使用示例

```typescript
import { createContinuousDevOrchestrator } from './blueprint';

// 创建编排器
const orchestrator = createContinuousDevOrchestrator({
  projectRoot: '/path/to/project',
  phases: {
    codebaseAnalysis: true,
    impactAnalysis: true,
    regressionTesting: true,
    cycleReset: true,
  },
  modelAssignment: {
    planner: 'opus',     // 规划用强模型
    worker: 'haiku',     // 执行用快模型
    reviewer: 'sonnet',  // 评审用平衡模型
  },
  humanCheckpoints: {
    beforeExecution: true,      // 执行前人工确认
    onHighRisk: true,           // 高风险时人工确认
    onRegressionFailure: true,  // 回归失败时人工介入
  },
});

// 处理新需求
const result = await orchestrator.processRequirement(
  "为系统增加用户邀请功能，支持邮件和链接邀请"
);

if (result.success) {
  console.log('蓝图已生成:', result.blueprint?.name);
  console.log('任务数:', result.taskTree?.stats.totalTasks);
  
  // 等待人工批准后执行
  await orchestrator.approveAndExecute();
}

// 监听事件
orchestrator.on('task_completed', (data) => console.log('任务完成:', data));
orchestrator.on('regression_failed', (data) => console.log('回归失败:', data));
orchestrator.on('cycle_reset', (data) => console.log('周期重置:', data));
```

---

## 📊 核心原则

1. **现有测试是最硬的护栏** - 永远不允许让现有测试失败
2. **新功能用新测试保护** - TDD，先写测试再写代码
3. **边界隔离** - Worker 只能在授权范围内操作
4. **影响可预测** - 任何改动前先分析影响范围
5. **可回滚** - 任何时候都能回到安全检查点
6. **周期性刷新** - 定期重置对抗漂移

---

## 🔗 相关文件

- `src/blueprint/types.ts` - 核心类型定义
- `src/blueprint/agent-coordinator.ts` - Agent 协调器（蜂王）
- `src/blueprint/worker-executor.ts` - Worker 执行器
- `src/blueprint/tdd-executor.ts` - TDD 循环执行器
- `src/blueprint/boundary-checker.ts` - 边界检查器
- `src/blueprint/impact-analyzer.ts` - 影响分析器（新）
- `src/blueprint/regression-gate.ts` - 回归测试门禁（新）
- `src/blueprint/cycle-reset-manager.ts` - 周期重置管理器（新）
- `src/blueprint/continuous-dev-orchestrator.ts` - 持续开发编排器（新）
